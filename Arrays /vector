#include<iostream>
# include<vector>
using namespace std;

void fun(int arr[],int n){
  for (int i=0;i<n;i++){
    cout<<arr[i]<<" "<<endl;
  }
}

void print(vector<int>v ){
  int size= v.size();
  for(int i=0;i< size;i++){
    cout<< v[i]<<endl;

  }
}

int main () {
  // static allocation
// int arr [5] = {1,2,3,4,5} ;
//   fun (arr,5);
  // cout<< "Enter the size of array"<<endl;
  //  int n;
  // cin>>n;
  // int *arr = new int[n];
  // // taking input and inserting in array
  // for(int i=0;i<n;i++){
  //   int data ;
  //   cin>> data;
  //   arr[i]= data;
  // }
  // cout<<" NOw the printing has started"<<endl;
  
  // for(int i=0;i<10;i++){
  //   arr[n+i]=80;
  // }
  // fun (arr,n);
//vector<int> v;
//   while(1){
//     int d; cin>>d;
//     v.push_back(d);
//     cout<<" Capacity"<<v.capacity()<<" "<<"size"<<v.size()<<endl;
//   }
 //  v.push_back(1);
 //   v.push_back(2);
 //   v.push_back(3);
 //  v.push_back(4);
 //   v.push_back(5);
  
 //  // print(v);
 // // pop
 //  v.pop_back();
 // int n; cin>> n;
 //  for(int i=0;i<n;i++){
 //    int d; cin>> d;
 //    v.push_back(d);
 //  }



  
 
 // for(int i=0;i<10;i++){
 //   v.push_back(55);
 //   v.push_back(20);
 // }
 // // to clear vector we can use 
 //  v.clear();
  
 //   print(v);


  // copy the vector
//   vector<int>v2{1,2,3,};
//   vector<int>v3(v2); //all elements of v2 are copied



  vector<int> arr;
  arr.push_back(10);

// vector<int> arr (5,-1);

  // vector<vector <int> > brr(1,vector<int>(15,-5));


  // for (int i=0;i<brr.size();i++){
  //   for(int j=0;j<brr[i].size();j++){
  //      cout << brr[i][j]<<" ";
  //   }
  //   cout<< endl;
  // }



  // // jagged Array
  // vector<vector<int> > arr;

  // vector<int> vec1(10,0);
  //  vector<int> vec2(5,2);
  //  vector<int> vec3(3,8);
  //  vector<int> vec4(1,10);
  //  vector<int> vec5(8,-5);

  // arr.push_back(vec1);
  // arr.push_back(vec2);
  // arr.push_back(vec3);
  // arr.push_back(vec4);
  // arr.push_back(vec5);
  
  // for (int i=0;i<arr.size();i++){
  //     for(int j=0;j<arr[i].size();j++){
  //        cout << arr[i][j]<<" ";
  //     }
  //     cout<< endl;
  //   }


  
}
